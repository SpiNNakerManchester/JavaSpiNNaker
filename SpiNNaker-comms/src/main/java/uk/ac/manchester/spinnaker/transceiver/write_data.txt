        log.info("Writing to " + core + " at " + baseAddress);
        log.info("" + data.remaining()+ " " + data.position() + " " + data.limit() + " " +  data.capacity()); 
        byte[] bytes = new byte[data.limit()];
        for (int i = 0; i < data.limit(); i++) {
            bytes[i]= data.get(i);
        }
        log.info(Arrays.toString(bytes));

